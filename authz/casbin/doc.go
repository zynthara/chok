// Package casbin doc.go is intentionally empty — the package-level
// godoc lives at the top of options.go where Builder, Service, and
// the Options type are introduced.
//
// The file's existence keeps the package importable even if every
// other source file is removed during a refactor.
package casbin
