http:
  addr: ":8080"

log:
  level: info
  format: json
  output:
    - stdout

database:
  driver: sqlite
  sqlite:
    path: "blog.db"

account:
  enabled: true
  signing_key: "CHANGE-ME-generate-with-openssl-rand-base64-32"

swagger:
  enabled: true
  title: "Blog API"
